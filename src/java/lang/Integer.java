package java.lang;

public class Integer {
    public static String toString(int value, int base) {
        /*int i = len;
        while(value > 0){
            // if len given limit to it, else continue
            if (len != 0 && i > 0){
                break;
            }
            int remainder = value % base;
            print(alphabet.charAt(remainder), x+i, y, color);

            i--;
            value = value / base;
        }*/
        return null;
    }
}
